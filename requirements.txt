# Memexai dependencies
# Updated: 2026-01-24
#
# Recommended: Use a virtual environment to avoid conflicts
#   python -m venv venv
#   venv\Scripts\activate  (Windows) or source venv/bin/activate (Mac/Linux)
#   pip install -r requirements.txt

# Web Framework
fastapi>=0.115.0
uvicorn>=0.32.0
pydantic>=2.0.0

# YouTube Data
scrapetube>=2.5.0  # Channel video discovery (no API key needed)
yt-dlp>=2026.6.9  # Public playlist fallback when YouTube markup breaks scrapetube
youtube-transcript-api>=1.0.0  # Transcript extraction

# LangChain + Google AI (Gemini API model wrappers)
# See: https://github.com/langchain-ai/langchain-google/discussions/1422
langchain-google-genai>=4.1.2  # Uses google-genai SDK for current Gemini API models

# Supabase
supabase>=2.0.0
python-jose[cryptography]>=3.3.0  # JWT validation
cryptography>=46.0.0  # BYOK encryption at rest in Supabase mode
stripe>=15.2.0  # Hosted Checkout, Customer Portal, and webhook verification

# Environment
python-dotenv>=1.0.0

# Tests
pytest>=9.0.0

# Code hygiene
ruff>=0.14.0
